@LIT_SITE_CFG_IN_HEADER@

# Tool-specific config options.
config.name_suffix = "@FLEXFAT_TEST_CONFIG_SUFFIX@"
config.target_cflags = "@FLEXFAT_TEST_TARGET_CFLAGS@"
config.clang = "@FLEXFAT_TEST_TARGET_CC@"
config.target_arch = "@FLEXFAT_TEST_TARGET_ARCH@"
# True when -DFLEXFAT_SIZES_CFG was set at cmake time.
config.flexfat_custom_config = @FLEXFAT_CUSTOM_CONFIG_ENABLED@

# Load common config for all compiler-rt lit tests.
lit_config.load_config(config, "@COMPILER_RT_BINARY_DIR@/test/lit.common.configured")

# Load tool-specific config that would do the real work.
lit_config.load_config(config, "@FLEXFAT_LIT_SOURCE_DIR@/lit.cfg.py")
